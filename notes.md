# My Notes

In VS Code, you can generate the `hashCode` and `equals` methods for a class by right clicking anywhere in the editor and selecting `Source Action...` > `Generate hashCode() and equals()...`.
